<main>
        <div class="p-3">
            <h2 class="text-center text-light">Available Watches</h2>
            <div class="container animate__animated animate__fadeInUp">
            <ol class="container ">
            {{#each watches as |watch|}}
            {{> available-cards}}
            {{/each}}
            </ol>    
            </div> 
            </div>
        </div>
    </main>